import { Component } from '@angular/core';

@Component({
  selector: 'app-root',
  standalone: true,
  template: '',
  styles: [
    ':host { display: block; min-height: 100vh; }'
  ]
})
export class AppComponent {}
